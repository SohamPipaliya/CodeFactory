@model CodeFactoryAPI.Models.Message

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Message</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="Message_ID" class="form-control" type="hidden" />
            <div class="form-group">
                <label asp-for="Messages" class="control-label"></label>
                <input asp-for="Messages" class="form-control" />
                <span asp-validation-for="Messages" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">User</label>
                @Html.DropDownList("User_ID", ViewBag.User_ID as SelectList, "Select User", new { @class = "form-control" })
                <span asp-validation-for="User_ID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Question</label>
                @Html.DropDownList("User_ID", ViewBag.Question_ID as SelectList, "Select Question", new { @class = "form-control" })
                <span asp-validation-for="Question_ID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
